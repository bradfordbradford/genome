!!! 5
= render 'ie-conditionals'
%html.no-js{:lang => "en"}

    %head
        = render 'head'

    %body

        -# Header & Content Wrap
        %section#header-content-wrap

            -# Header & Navigation
            = render 'header-primary'

            -# Content Wrap
            #content-wrap

                -# Main
                %main
                    = yield

        -# Site Footer
        %footer#main-footer
            = render 'footer-primary'

        = render 'bodyscripts'
